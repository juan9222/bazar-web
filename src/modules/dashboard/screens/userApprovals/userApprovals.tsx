import React from "react";
import Button from 'react-bootstrap/Button';

const UserApprovals: React.FC<any> = () => {
  return (
    <div>
      <Button variant="primary">User Approvals</Button>{ ' ' }
    </div>
  );
};

export default UserApprovals;